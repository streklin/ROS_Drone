#include<opencv2/opencv.hpp>
#include<ros/ros.h>

using namespace cv;

int main(int argc, char **argv) {
    // initialize ROS
    
    // create a node handle
    
    // create video capture object (opencv2)
    
    // create a publisher
    
    // publish the current image at the specified framerate
    // (or as closely as possible)
        // collect the image from open cv
        // DEBUG: show the frame being collected.
        // publish the message
}